The SALT compiler rejected your specification.

Your specification:
```
{SPEC}
```

Compiler error:
```
{COMPILE_ERROR}
```

Fix the specification. Common causes: an undeclared or misspelled atom,
a reserved keyword used as an identifier (quote it), timed syntax (not
allowed), or operator misuse — re-check the reference excerpts above.

OUTPUT: ONLY the corrected SALT specification text. No markdown fences,
no explanations.
