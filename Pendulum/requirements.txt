# Pendulum runtime dependencies.
# Install into the local venv:  .venv/bin/pip install -r requirements.txt
# Versions are floored at the majors verified during initial development
# (July 2026); freeze harder if reproducibility becomes an issue.

pydantic-ai-slim[openai,mcp]>=0.4   # agents + MCP stdio client + OpenAI-compat models
langgraph>=0.5                      # fixed-workflow graph runtime (Send API, reducers)
httpx>=0.27                         # direct LLM HTTP (grammar + logprobs) and embeddings
numpy>=2.0                          # RAG vector math
python-dotenv>=1.0                  # .env loading
pypdf>=5.0                          # SALT manual PDF extraction (RAG build only)

# Testing
pytest>=8.0
pytest-asyncio>=0.24
